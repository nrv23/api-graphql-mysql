# Comments in GraphQL strings (such as this one) start with the hash (#) symbol.

type Query {
  #lista de usuarios  
  users: [User]
  user(id: ID!): User

  # --------------------

  languages: [Language]
  language(id: ID!): Language
}


